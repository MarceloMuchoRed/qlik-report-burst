# --- Run output ---------------------------------------------------------
screenshots/

# --- Recipient data --------------------------------------------------------
# recipients.xlsx is committed with DUMMY test data so the repo runs on clone.
# Replace it with the real list before going live (note: it IS tracked, so a
# real list committed here lands in the repo). Other formats stay ignored:
recipients.xlsm
recipients.csv

# --- Python --------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
